# runtime artifacts (generated by the pipeline / local dev)
pipeline/data/
pipeline/dist/
container/pipeline
container/dist
*.log
.DS_Store
config/
seed-imgs/

# screenshot staging for PR before/after evidence
captures/

__pycache__/

# local build output at the repo root (the real dist lives in pipeline/dist)
/dist/

# the local Lavish review artifact (built from dist; not source)
.lavish/phone/
